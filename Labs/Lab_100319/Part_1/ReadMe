  # Terminal: git clone https://github.com/danspaes/db-sandbox

# Enter into the cloned directory
  # Terminal : cd db-sandbox

# Start your environment
  # Terminal : docker-compose up -d

# Log into your mongo image with the command
  # Terminal : docker exec -it redis /bin/bash

# Log into redis instance
  # Terminal :  redis-cli

# Ping input the terminal, PONG should be output
  # Terminal: ping
